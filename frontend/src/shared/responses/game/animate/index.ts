import {death} from "./death";
import {floatingText} from "./floatingText";
import {number} from "./number";
import {shake} from "./shake";

const animate = {death, floatingText, number, shake};

export {animate};
